<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Đăng nhập - WebView</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="min-h-screen flex items-center justify-center bg-[#f0f4f8] font-sans"
  >
    <div class="w-full max-w-sm bg-white p-6 rounded-xl shadow-md">
      <h2 class="text-xl font-bold text-center text-gray-800 mb-4">Lịch Học</h2>
      <p class="text-sm text-center text-gray-500 mb-6">
        Đăng nhập bằng tài khoản <strong>Đăng ký tín chỉ</strong>
      </p>

      <% if (typeof error !== 'undefined') { %>
      <p class="text-red-500 text-sm text-center mb-4"><%= error %></p>
      <% } %>

      <form action="/login" method="POST" class="space-y-4">
        <input
          type="text"
          name="mssv"
          placeholder="Mã sinh viên"
          class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
          required
        />
        <input
          type="password"
          name="matkhau"
          placeholder="Mật khẩu"
          class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400"
          required
        />
        <button
          type="submit"
          class="w-full bg-blue-500 text-white py-2 rounded-lg hover:bg-blue-600 transition"
        >
          Đăng Nhập
        </button>
      </form>
    </div>
  </body>
</html>
